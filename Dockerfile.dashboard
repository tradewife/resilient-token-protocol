FROM node:20-slim AS deps
WORKDIR /app
COPY sdk/package.json sdk/package.json
COPY sdk/idl.ts sdk/idl.ts
COPY sdk/README.md sdk/README.md
COPY dashboard/package.json dashboard/package-lock.json dashboard/
RUN cd dashboard && npm ci

FROM node:20-slim AS builder
WORKDIR /app
COPY --from=deps /app/dashboard/node_modules dashboard/node_modules
COPY --from=deps /app/sdk sdk
COPY dashboard/ dashboard/
RUN cd dashboard && npm run build

FROM node:20-slim AS runner
# v2: fixed static asset paths (.next/static not dashboard/.next/static)
WORKDIR /app
ENV NODE_ENV=production
ENV HOSTNAME="0.0.0.0"
ENV PORT=3000
COPY --from=builder /app/dashboard/.next/standalone ./
COPY --from=builder /app/dashboard/.next/static .next/static
COPY --from=builder /app/dashboard/public ./public
EXPOSE 3000
# HEALTHCHECK: Railway injects PORT=8080 at runtime. The standalone Next.js server
# respects $PORT from ENV — Railway overwrites our ENV PORT=3000 with PORT=8080.
# Probe the dynamically-injected port so Railway sees 200 instead of failing.
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD node -e "require('http').get('http://localhost:' + (process.env.PORT || '3000') + '/', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"
CMD ["node", "server.js"]
