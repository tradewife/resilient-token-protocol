# Known-and-tolerated peer conflict (see CLAUDE.md): @coral-xyz/anchor ^0.32
# vs the sdk's peer ^0.31. Every npm install in dashboard/ (including tooling
# that shells out to npm, e.g. the shadcn CLI) must use legacy resolution.
legacy-peer-deps=true
